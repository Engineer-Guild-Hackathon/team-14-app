# Docker環境用の環境変数
# 本番環境では必ず変更してください

# JWT Secrets (本番では強力なランダム文字列に変更)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production

# OpenAI API Key (実際のキーを設定)
OPENAI_API_KEY="your_openai_api_key"

# Chrome Extension ID (実際のExtension IDを設定)
CHROME_EXTENSION_ID=your-chrome-extension-id

# Database (docker-composeで自動設定されますが、オーバーライド可能)
POSTGRES_DB=codeclimb
POSTGRES_USER=codeclimb_user
POSTGRES_PASSWORD=codeclimb_password

# Additional settings (オプション)
MAX_FILE_SIZE=10485760
WS_CORS_ORIGIN=http://localhost:3001
FRONTEND_URL=http://localhost:3001
